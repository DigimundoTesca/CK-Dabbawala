# Local development dependencies go here
-r base.txt

# Django Debug
django-debug-toolbar==1.7
django-extensions==1.7.8

# libraries to creating and executing simple grammars
pyparsing==2.2.0

# Before install unix requirements, you ned to run the next system packages install
# sudo apt-get install graphviz libgraphviz-dev graphviz-dev pkg-config

# Python interface to Graphviz's Dot
pydot==1.2.3

# Python interface to Graphviz's Dot language
pydotplus==2.0.2

# Python interface to the Graphviz graph layout and visualization package.
pygraphviz==1.3.1
graphviz==0.8.2
