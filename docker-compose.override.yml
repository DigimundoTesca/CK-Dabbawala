version: '3.0'

services:
  db:

  web:
    build:
      args:
        - DJANGO_ENV=local-unix
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/CK-Dabbawala
    ports:
      - "8000:8000"
    depends_on:
      - db

volumes:
  db_data:
    external: true